<div class="col-4-7">
    <h2>{{ item.name }}</h2>
    <p>{{ item.description }}</p>
    <p><a
        href="{{ url('order:add', {'product' : item.id}) }}">
        <button class="btn small solid red">{{ string('buttons.order', item.name) }}</button></a></p>
</div>